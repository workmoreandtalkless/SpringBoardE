/** #1
 * [1,2,3,4]
 * ans {1,2,3,4}
 */

 /** #2
  * ?
  * referee
  */

/** #3
 * { [1,2,3] : false
 * }
 * 
 * 0: {Array(3) => true}
  1: {Array(3) => false}
 */

/** hasDuplicate
 * 
 */
function hasDuplicate(arr){
    const set = new set(arr);
    return set.size !== arr.size;
}

/** vowelCount
 * 
 */

 function vowelCount(str){
     let map = new map();
     for(let s of str){
         map.add(s,)
     }
 }